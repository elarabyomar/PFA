from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import text
from fastapi import HTTPException, status

# Service vide - toutes les fonctions de changement de mot de passe ont été supprimées 